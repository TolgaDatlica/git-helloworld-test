version: "3.9"

services:
  web:
    build: .
    container_name: hello-world-web
    restart: always
    ports:
      - "80:9090"
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
